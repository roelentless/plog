[package]
name = "plog"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4"
serde_json = "1"
signal-hook = "0.3"
libc = "0.2"

[dev-dependencies]
tempfile = "3"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
